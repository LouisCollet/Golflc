<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>lc</groupId>
    <artifactId>GolfWfly</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>war</packaging>
    <name>GolfJakarta</name>
    <description> this is the GolfLC applications's description</description>

    <properties>
        <env>environnement de LC</env>
        <endorsed.dir>${project.build.directory}/endorsed</endorsed.dir>
        <directory>${project.basedir}/target</directory>
        <finalName.version>${project.artifactId}-${project.version}</finalName.version>
        <sourceDirectory>${project.basedir}/src/main/java</sourceDirectory>
        <build>${project.build.sourceDirectory}</build> 
        <outputDir>${project.build.outputDirectory}</outputDir>  
        <build>${maven.build.timestamp}</build> 
<!--       <maven.build.timestamp.format>yyyyMMdd'T'HHmmss</maven.build.timestamp.format> -->
 <!--       <maven.build.timestamp.format>dd-MM-yyyy HH:mm:ss</maven.build.timestamp.format> 
        <maven.build.timestamp.format>yyyy-MM-dd HH:mm:ss z</maven.build.timestamp.format> -->
        <maven.build.timestamp.format>yyyy-MM-dd HH:mm:ss</maven.build.timestamp.format>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding> <!-- used by surefire -->
        <java.version>26</java.version> <!-- mod 29-09-2025 !! used by compiler was 23  -->
        <jakartaee-api.version>11.0.0</jakartaee-api.version> <!-- mod 08-07-2025 was 10.0.0 // mod 30-07-2023 was 8.0.0 !! the great migration
            https://github.com/jakartaee/platform/blob/main/namespace/unaffected-packages.adoc -->
        <primefaces.version>15.0.14</primefaces.version> <!-- mod 01-04-2026 was 15.0.13 -->
        <primefaces-extension.version>15.0.14</primefaces-extension.version> <!-- mod 01-04-2026 was 15.0.13-->
        <omnifaces.version>5.2.2</omnifaces.version> <!-- mod 06-04-2026 was 5.1 -->
        <chartjs.version>3.0.1</chartjs.version> <!-- mod 16-03-2026 was 2.9.0 -->
        <mockito.version>5.21.0</mockito.version> <!-- mod 29-12-2025 was 5.20.0-->
   <!-- Official Maven Plugins --> 
        <maven.version>4.0.0-rc-5</maven.version> <!-- mod 25-11 was 4.0.0-rc-4  -->
        <maven-compiler-plugin.version>3.15.0</maven-compiler-plugin.version><!-- mod 16-03-2026 was 3.14.1 -->
        <maven-dependency-plugin.version>3.9.0</maven-dependency-plugin.version> <!-- mod 24-10-2024 was 3.8.1  -->
        <maven-clean-plugin.version>3.5.0</maven-clean-plugin.version> <!--  mod 13-06-2025 was 3.4.1-->
        <maven-resources-plugin.version>3.5.0</maven-resources-plugin.version><!-- mod 16-03-2026  was 3.4.0 -->
        <maven-war-plugin.version>3.5.1</maven-war-plugin.version> <!-- mod 09-12-2025  was 3.5.0 -->
        <maven-install-plugin.version>3.1.4</maven-install-plugin.version> <!-- mod 12-02-2025 was 3.1.3 -->
        <maven-properties-plugin.version> </maven-properties-plugin.version>
        <maven-surefire-plugin.version>3.5.5</maven-surefire-plugin.version><!-- mod 16-03-2026  was 3.5.4 -->
        <maven-wildfly-plugin.version>6.0.0.Beta2</maven-wildfly-plugin.version> <!-- mod 16-03-2026 was 5.1.5.Final -->
        <wildfly-hostname>localhost</wildfly-hostname>  <!-- was 127.0.0.1   essai avec default ??-->
        <wildfly-port>8080</wildfly-port>
      <!--  <wildfly.home>c:/ProgramData/wildfly-35.0.0.Final4</wildfly.home>  new 03/11/20224 used in ??? -->
      <!--   <wildfly.version>35</wildfly.version> new 03/11/2024 not used in  -->
        <maven-enforcer-plugin.version>3.6.2</maven-enforcer-plugin.version> <!-- mod 25-10-2025 was 3.6.1 -->
        <maven-exec-plugin.version>3.6.3</maven-exec-plugin.version><!-- mod 04-01-2026 was 3.6.2 -->

  <!--   <exec.file>mongo.MongoHelpLoad</exec.file> utils.Java11HttpClientExample-->
        <exec.mainClass.pom>read.ReadClub</exec.mainClass.pom> <!-- dao.TestClubDAOMainical.icalservice_manual_test utils.CdiAuditScannern dao.TestClubDAOImpl utils.IcalServicetest.prepareStatement.ClubService>lists.CourseListupdate.UpdateClubsql.SqlFactoryutils.DBMetajollyday.JollyDayControllers.HttpController selenium.SeleniumTest  ScreenShotCaptureSelenium execute le main de la classe (qu'il ne faut pas mentionner) -->
        <!-- find.FindTarifMembersData Controllers.MongoHelpController to run with icon toolbar -->

        <junit-jupiter.version>6.0.3</junit-jupiter.version><!-- mod 16-03-2026 was 6.0.2-->
        <junit-platform.version>1.10.2</junit-platform.version>
     <!--   <junit-jupiter.version>5.10.5</junit-jupiter.version> mod 17-11-2025 was 6.0.0-->
    <!--    <junit.jupiter.version>5.10.1</junit.jupiter.version>  -->
        <failsafe.version>3.5.5</failsafe.version>  <!-- mod 16-03-2026 was 3.5.4 -->
        <arquillian.version>1.10.1.Final</arquillian.version>  <!-- mod 23-03-2026 was 1.10.0.Final-->
        <wildfly.version>31.0.0.Beta3</wildfly.version> <!-- new 06-01-2025-->
        <shrinkwrap.version>1.2.6</shrinkwrap.version>
        
  <!-- no test with this property https://www.baeldung.com/maven-skipping-tests -->
     <!--   <tests.skip>false</tests.skip>  mod 08-09-2022  if true, tests are skipped -->
        
        <skip.unit.tests>true</skip.unit.tests>  <!--  surefire -->
        <skip.integration.tests>false</skip.integration.tests>  <!-- failsafe  -->
        <!-- Pourquoi ? Parce que -DskipTests est une propriété système reconnue par Surefire ET Failsafe.  -->
        
        <test.file>calc.GroupedTests.java</test.file>  <!-- file to be tested with icon toolbar -->
     <!--   <testng.version>7.11.0</testng.version>  mod 02-03-2025 xas 7.10.2 --> 
   <!--      <project.build.testOutputDirectory>src/test/java</project.build.testOutputDirectory>  this is the default  -->
        <maven.archiver.plugin.version>3.6.2 </maven.archiver.plugin.version><!--mod 17-07-2024 was 3.6.0 -->
        <maven.build.helper.plugin.version>3.6.1</maven.build.helper.plugin.version> <!--mod 13-06-2025 was 3.6.0 -->
    <!--    <netbeans.compile.on.save>all</netbeans.compile.on.save>  new 27/11/2013 enlevé 08-08-2024-->
        <netbeans.version>28</netbeans.version> <!-- only used via MANIFEST.MF see manifestEntries further-->
        <primeflex.version>4.0.0</primeflex.version> <!-- modify also masterTemplate - mod 16-03-2025 was 3.3.1 used also in technical info MANIFEST.MF see manifestEntries further-->
        <log4j2.version>2.25.4</log4j2.version> <!-- mod 04-03-2026  was 2.25.4 -->
        <slf4j.version>2.1.0-alpha1</slf4j.version><!-- mod 12-03-2025 was 2.0.163 -->
        <jackson.version>2.21.2</jackson.version> <!--mod 03-04-2026 was 2.21.1 -->
        <mysql-connector-j.version>9.6.0</mysql-connector-j.version>  <!-- used by RUN FILE mod 06-05-2025 was version 9.2.0-->
        <resteasy.version>7.0.1.Final</resteasy.version> <!--  mod 27-12-2025 was 7.0.0.Final -->
        <version.server.bom>26.1.3.Final</version.server.bom>  <!--mod 11-12-2023 was 26.1.2.Final -->
<!-- new 14/08/2022 -->
        <mongodb-driver-sync.version>5.6.4</mongodb-driver-sync.version> <!-- mod 16-03-2026 was 5.6.2 -->
       <!-- <mongodb-driver-reactivestrings.version>4.11.1</mongodb-driver-reactivestrings.version> mod 11-12-2023 was 4.11.0 -->
        <mongodb-crypt.version>5.6.2</mongodb-crypt.version><!--  mod 27-12-2025 was 5.6.1 -->
        <protoc.version>1.78.0</protoc.version> <!--  mod 04-01-2026 was 1.77.0 -->
      <!--  <grpc.version>4.33.2</grpc.version>   mod 27-12-2025 was 4.33.1 
                  https://github.com/grpc/grpc-java/issues/11015   -->
        <exec.inheritIo>true</exec.inheritIo><!--  mod 19-03-2023 fix NetBeans IDE pour voir la sortie support ansi color de mvn exec:java dans la console https://github.com/apache/netbeans/issues/5552 -->
        <selenium.version>4.41.0</selenium.version> <!--  mod 16-03-2026 was 4.39.0 -->
        <jollyday.version>2.5.0</jollyday.version><!--  mod 17-04-2026 was 2.4.0 -->
        <weld.version>6.0.4.Final</weld.version> <!--  pour refactoring CDI -->
 
    </properties>

 <dependencies>

  <!-- Jakarta EE 11 platform API — CDI, Servlet, JSF, JPA, JAX-RS, etc. — provided by WildFly at runtime -->
<dependency>
    <groupId>jakarta.platform</groupId>
    <artifactId>jakarta.jakartaee-api</artifactId>
    <version>${jakartaee-api.version}</version>
    <scope>provided</scope>
</dependency>

<!-- OmniFaces 5 — JSF utilities: conditional rendering, converters, validators, combined resource handler -->
<dependency>
    <groupId>org.omnifaces</groupId>
    <artifactId>omnifaces</artifactId>
    <version>${omnifaces.version}</version>
</dependency>
<!-- PrimeFaces Extensions — additional UI components: Timeline, CodeEditor, Sheet, etc. -->
<dependency>
    <groupId>org.primefaces.extensions</groupId>
    <artifactId>primefaces-extensions</artifactId>
    <version>${primefaces-extension.version}</version>

    <classifier>jakarta</classifier>
</dependency>
<!-- Chart.js Java model — server-side chart data building for p:chart (PrimeFaces 15+) -->
<dependency>
   <groupId>software.xdev</groupId>
   <artifactId>chartjs-java-model</artifactId>
   <version>${chartjs.version}</version>
 </dependency>

<!-- PrimeFaces 15 — main JSF UI component library (dataTables, dialogs, calendars, etc.) -->
<dependency>
    <groupId>org.primefaces</groupId>
    <artifactId>primefaces</artifactId>
    <version>${primefaces.version}</version>
    <classifier>jakarta</classifier>
</dependency>

<!-- OWASP HTML Sanitizer — required by PrimeFaces 15 p:textEditor secure="true" -->
<dependency>
    <groupId>com.googlecode.owasp-java-html-sanitizer</groupId>
    <artifactId>owasp-java-html-sanitizer</artifactId>
    <version>20260313.1</version>
</dependency>

<!-- Log4j 2 API — logging interface used throughout the application (LOG.debug/info/warn/error) -->
<dependency>
    <groupId>org.apache.logging.log4j</groupId>
    <artifactId>log4j-api</artifactId>
    <version>${log4j2.version}</version> 
</dependency>

<!-- Log4j 2 core — logging runtime implementation (config: src/main/resources/log4j2.xml) -->
<dependency>
    <groupId>org.apache.logging.log4j</groupId>
    <artifactId>log4j-core</artifactId>
    <version>${log4j2.version}</version> 
</dependency> 

<!-- Apache POI — Excel XLS support (base library, required by poi-ooxml) -->
<dependency>
    <groupId>org.apache.poi</groupId>
    <artifactId>poi</artifactId>
    <version>5.5.1</version> <!-- mod 27-12-2025 was 5.5.0 -->
</dependency>

<!-- Apache POI OOXML — Excel XLSX export used by PrimeFaces p:dataExporter -->
<dependency>
    <groupId>org.apache.poi</groupId>
    <artifactId>poi-ooxml</artifactId>
    <version>5.5.1</version> <!-- mod 27-12-2025 was 5.5.0  -->
</dependency>

<!-- Apache Commons IO — required by Apache POI (UnsynchronizedByteArrayOutputStream) -->
<dependency>
    <groupId>commons-io</groupId>
    <artifactId>commons-io</artifactId>
    <version>2.21.0</version> <!-- mod 17-11-2025 was 2.20.0  -->
</dependency>

<!-- Jackson core — JSON streaming API (low-level tokenizer, required by jackson-databind) -->
<dependency>
  <groupId>com.fasterxml.jackson.core</groupId>
  <artifactId>jackson-core</artifactId>
  <version>${jackson.version}</version>
</dependency> 

<!-- Jackson annotations — @JsonProperty, @JsonIgnore, etc. (used in REST/payment DTOs) -->
<dependency>
    <groupId>com.fasterxml.jackson.core</groupId>
    <artifactId>jackson-annotations</artifactId>
    <version>2.21</version>
</dependency>

<!-- Jackson JSR-310 module — serialization of LocalDate/LocalDateTime/ZonedDateTime -->
<dependency>
    <groupId>com.fasterxml.jackson.datatype</groupId>
    <artifactId>jackson-datatype-jsr310</artifactId>
    <version>${jackson.version}</version>
</dependency>
<!-- Jackson databind — ObjectMapper: JSON serialization/deserialization (REST payloads, MongoDB) -->
<dependency>
    <groupId>com.fasterxml.jackson.core</groupId>
    <artifactId>jackson-databind</artifactId>
    <version>${jackson.version}</version>
</dependency>

<!-- Google Maps Services — geocoding API used to find GPS coordinates from club address -->
<dependency>
    <groupId>com.google.maps</groupId>
    <artifactId>google-maps-services</artifactId>
    <version>2.2.0</version>  <!-- mod 23-06-2023 was 2.0.0 -->
</dependency>

<!-- OpenCSV — CSV parsing and generation (player import/export) -->
<dependency>
    <groupId>com.opencsv</groupId>
    <artifactId>opencsv</artifactId>
    <version>5.12.0</version> <!--   mod 31-07-2025 was 5.11.2 -->
</dependency>

<!-- MySQL Connector/J — JDBC driver for golflc database at port 3308 -->
<dependency>
    <groupId>com.mysql</groupId>
    <artifactId>mysql-connector-j</artifactId>
    <version>${mysql-connector-j.version}</version>
    <scope>compile</scope>
</dependency>

<!-- OpenPDF — PDF generation used by PrimeFaces p:dataExporter (fork of iText with com.lowagie.* API) -->
<dependency>
    <groupId>com.github.librepdf</groupId>
    <artifactId>openpdf</artifactId>
    <version>2.0.3</version><!-- PrimeFaces 15 needs com.lowagie.* package — OpenPDF 3.x renamed to com.github.librepdf -->
</dependency>

<!-- iCal4j — iCalendar (.ics) generation for round/lesson scheduling exports -->
<dependency>
    <groupId>org.mnode.ical4j</groupId>
    <artifactId>ical4j</artifactId>
    <version>4.2.5</version> <!-- mod 17-04-2026 was 4.2.4-->
</dependency>
<!-- iCal4j vCard — vCard (.vcf) contact generation (player contact export) -->
<dependency>
    <groupId>org.mnode.ical4j</groupId>
    <artifactId>ical4j-vcard</artifactId>
    <version>2.1.0</version> <!-- mod 04-01-2026 was 2.0.1 -->
</dependency>

<!-- Apache Tika core — MIME type detection for file upload validation (FileUploadController) -->
<dependency>
    <groupId>org.apache.tika</groupId>
    <artifactId>tika-core</artifactId>
    <version>3.3.0</version><!-- mod 07-04-2026 was 3.2.32-->
</dependency>

<!-- Thumbnailator — image thumbnail generation for player/club photo uploads -->
<dependency>
    <groupId>net.coobird</groupId>
    <artifactId>thumbnailator</artifactId>
    <version>0.4.21</version> <!-- mod 24-10-2025 was 0.4.20-->
</dependency>

<!-- JUnit 5 API — test annotations (@Test, @BeforeEach, etc.) -->
<dependency>
    <groupId>org.junit.jupiter</groupId>
    <artifactId>junit-jupiter-api</artifactId>
    <version>${junit-jupiter.version}</version>
    <scope>test</scope>
</dependency>
<!-- JUnit 5 engine — test runner (required by Surefire/Failsafe to execute Jupiter tests) -->
<dependency>
    <groupId>org.junit.jupiter</groupId>
     <artifactId>junit-jupiter-engine</artifactId>
    <version>${junit-jupiter.version}</version>
    <scope>test</scope>
</dependency>
<!-- JUnit 5 parameterized tests — @ParameterizedTest, @CsvSource, @MethodSource -->
<dependency>
	<groupId>org.junit.jupiter</groupId>
	<artifactId>junit-jupiter-params</artifactId>
	<version>${junit-jupiter.version}</version>
	<scope>test</scope>
</dependency>

<!-- Apache Commons Lang 3 — string utilities, reflection helpers, date utils (StringUtils, etc.) -->
<dependency>
    <groupId>org.apache.commons</groupId>
    <artifactId>commons-lang3</artifactId>
    <version>3.20.0</version>  <!-- mod 25-11-2025 was 3.19.0  -->
</dependency>

<!-- Google Cloud Translation API — auto-translate validation/UI messages to supported locales -->
<dependency>
    <groupId>com.google.cloud</groupId>
    <artifactId>google-cloud-translate</artifactId>
    <version>2.88.0</version> <!-- mod 03-04-2026 was 2.86.0 -->
</dependency>


<!-- Quartz Scheduler — cron-like job scheduling (SchedulerProController, subscription renewal) -->
<dependency>
    <groupId>org.quartz-scheduler</groupId>
    <artifactId>quartz</artifactId>
    <version>2.5.2</version>
</dependency>
<!-- Quartz built-in jobs — standard job implementations bundled with Quartz -->
<dependency>
    <groupId>org.quartz-scheduler</groupId>
    <artifactId>quartz-jobs</artifactId>
    <version>2.5.2</version>
</dependency>

<!-- QRGen JavaSE (QR code generator) -->
    <dependency>
      <groupId>com.github.kenglxn.QRGen</groupId>
      <artifactId>javase</artifactId>
      <version>3.0.1</version>
    </dependency>

<!-- MongoDB BSON — BSON types (Document, ObjectId) used in MongoHelpController -->
<dependency>
    <groupId>org.mongodb</groupId>
    <artifactId>bson</artifactId>
    <version>5.6.4</version> <!-- mod 16-03-2026 was 5.6.2  -->
</dependency>

<!-- MongoDB synchronous driver — MongoClient, MongoCollection queries (MongoHelpController) -->
<dependency>
    <groupId>org.mongodb</groupId>
    <artifactId>mongodb-driver-sync</artifactId>
    <version>${mongodb-driver-sync.version}</version>
</dependency>

<!-- mongodb-driver-core désactivé 2026-04-03 — déjà tiré transitivement par mongodb-driver-sync,
     aucun import direct dans le code. Laisser Maven gérer la version via mongodb-driver-sync.
<dependency>
    <groupId>org.mongodb</groupId>
    <artifactId>mongodb-driver-core</artifactId>
    <version>${mongodb-driver-sync.version}</version>
</dependency>
-->

<!-- PrimeFlex — CSS utility library (flex grid, spacing, responsive) — served as WebJar -->
<dependency>
   <groupId>org.webjars.npm</groupId>
   <artifactId>primeflex</artifactId>
   <version>${primeflex.version}</version>
</dependency>

<!-- SLF4J API — logging facade required by JollyDay and MongoDB driver (bridged to Log4j 2) -->
<dependency>
    <groupId>org.slf4j</groupId>
    <artifactId>slf4j-api</artifactId>
    <version>${slf4j.version}</version>
    <scope>compile</scope>
</dependency>

<!-- nécessaire  
<dependency>
    <groupId>org.jboss.as</groupId>
    <artifactId>jboss-as-controller-client</artifactId>
    <version>7.2.0.Final</version>
</dependency>
-->
<!--enlevé 10- 1-2026
<dependency>
    <groupId>io.grpc</groupId>
    <artifactId>grpc-netty</artifactId>
    <version>1.78.0</version> 
    <scope>provided</scope>>
</dependency>
   
<dependency>
  <groupId>com.google.protobuf</groupId>
  <artifactId>protobuf-java</artifactId>
  <version>${grpc.version}</version>
</dependency>


<dependency>
    <groupId>com.google.protobuf</groupId>
    <artifactId>protobuf-java-util</artifactId>
    <version>${grpc.version}</version>
</dependency><dependency>
    <groupId>io.grpc</groupId>
    <artifactId>grpc-protobuf</artifactId>
    <version>${protoc.version}</version> 
</dependency>

<dependency>
    <groupId>io.grpc</groupId>
    <artifactId>grpc-stub</artifactId>
    <version>${protoc.version}</version>
</dependency>
-->
<!-- enlevé 30-12-2025 necessary for Java 9+
   <dependency> 
            <groupId>org.apache.tomcat</groupId>
            <artifactId>annotations-api</artifactId>
            <version>6.0.53</version>
            <scope>provided</scope>
        </dependency>
  -->       
<!-- mod 16-02-2024 https://mvnrepository.com/artifact/jakarta.activation/jakarta.activation-api -->
<!-- https://mvnrepository.com/artifact/jakarta.mail/jakarta.mail-api -->

<!--
<dependency>
    <groupId>jakarta.mail</groupId>
    <artifactId>jakarta.mail-api</artifactId>
    <version>2.1.5</version> 
</dependency>
-->

<!-- Eclipse Angus Mail — Jakarta Mail implementation used to send emails (activation, password reset) -->
<dependency>
    <groupId>org.eclipse.angus</groupId>
    <artifactId>jakarta.mail</artifactId>
    <version>2.0.5</version> <!-- mod 24-10-2025 was 2.0.4  -->
</dependency>

<!-- Dropwizard Metrics core — required transitively by MongoDB driver sync -->
<dependency>
    <groupId>io.dropwizard.metrics</groupId>
    <artifactId>metrics-core</artifactId>
    <version>4.2.38</version><!-- mod 16-03-2026 was 4.2.37  -->
</dependency>

<!-- Selenium WebDriver Java — E2E browser automation base library -->
<dependency>
    <groupId>org.seleniumhq.selenium</groupId>
    <artifactId>selenium-java</artifactId>
    <version>${selenium.version}</version>
    <scope>test</scope>
</dependency>
<!-- Selenium Chrome driver — ChromeDriver WebDriver implementation for E2E tests -->
<dependency>
    <groupId>org.seleniumhq.selenium</groupId>
    <artifactId>selenium-chrome-driver</artifactId>
    <version>${selenium.version}</version>
    <scope>test</scope>
</dependency>
<!-- Selenium Firefox driver — GeckoDriver implementation (multi-browser E2E tests) -->
<dependency>
    <groupId>org.seleniumhq.selenium</groupId>
    <artifactId>selenium-firefox-driver</artifactId>
    <version>${selenium.version}</version>
    <scope>test</scope>
</dependency>
<!-- Selenium Edge driver — EdgeDriver implementation (multi-browser E2E tests) -->
<dependency>
    <groupId>org.seleniumhq.selenium</groupId>
    <artifactId>selenium-edge-driver</artifactId>
    <version>${selenium.version}</version>
    <scope>test</scope>
</dependency>
<!-- Selenium DevTools v133 — Chrome DevTools Protocol support for network/console inspection -->
<dependency>
    <groupId>org.seleniumhq.selenium</groupId>
    <artifactId>selenium-devtools-v133</artifactId>
    <version>4.31.0</version>
    <scope>test</scope>
</dependency>

<!-- WebDriverManager — automatic ChromeDriver/GeckoDriver binary management for Selenium tests -->
<dependency>
    <groupId>io.github.bonigarcia</groupId>
    <artifactId>webdrivermanager</artifactId>
    <version>6.3.4</version> <!-- mod 17-04-2026 was 6.3.3 -->
    <scope>test</scope>
</dependency>

<!-- selenium-jupiter — JUnit 5 extension for Selenium: @SeleniumJupiter, driver injection -->
<dependency>
    <groupId>io.github.bonigarcia</groupId>
    <artifactId>selenium-jupiter</artifactId>
    <version>6.3.1</version> <!-- mod 29-09-2025 was 6.2.0 -->
    <scope>test</scope>
</dependency>

<!-- AssertJ — fluent assertion library used in unit and integration tests -->
<dependency>
    <groupId>org.assertj</groupId>
    <artifactId>assertj-core</artifactId>
    <version>3.27.7</version> <!-- new 16-03-2026 was 3.27.6 -->
    <scope>test</scope>
</dependency>

<!-- AShot — full-page screenshot utility for Selenium E2E tests -->
<dependency>
    <groupId>ru.yandex.qatools.ashot</groupId>
    <artifactId>ashot</artifactId>
    <version>1.5.4</version>
    <scope>test</scope>
</dependency>

<!-- PrimeFaces Selenium core — base framework for PrimeFaces component testing -->
<dependency>
    <groupId>org.primefaces</groupId>
    <artifactId>primefaces-selenium-core</artifactId>
    <version>${primefaces.version}</version>
    <scope>test</scope>
</dependency>

<!-- PrimeFaces Selenium components — typed wrappers for PrimeFaces UI components in tests -->
<dependency>
    <groupId>org.primefaces</groupId>
    <artifactId>primefaces-selenium-components</artifactId>
    <version>${primefaces.version}</version>
    <scope>test</scope>
</dependency>

<!-- urlbuilder — fluent URL builder used in PaymentRestResource payment callbacks -->
<dependency>
    <groupId>io.mikael</groupId>
    <artifactId>urlbuilder</artifactId>
    <version>2.0.9</version>
</dependency>

<!-- Mockito core — mock framework for unit tests (CDI beans, services) -->
<dependency>
    <groupId>org.mockito</groupId>
    <artifactId>mockito-core</artifactId>
    <version>5.23.0</version>  <!-- mod 23-03-2026 was 5.22.0 -->
    <scope>test</scope>
</dependency>
<!-- https://mvnrepository.com/artifact/org.mockito/mockito-junit-jupiter 
<dependency>
    <groupId>org.mockito</groupId>
    <artifactId>mockito-junit-jupiter</artifactId>
    <version>${mockito.version}</version>
    <scope>test</scope>
</dependency>
-->
<!-- new 12-12-2025 jours fériés https://mvnrepository.com/artifact/de.jollyday/jollyday -->
<dependency>
    <groupId>de.focus-shift</groupId>
    <artifactId>jollyday-core</artifactId>  <!--  was jollyday-core -->
    <version>${jollyday.version}</version>
    <!--  <exclusions>
       Exclure SLF4J Simple pour éviter les warnings 
            <exclusion>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-simple</artifactId>
            </exclusion>
    </exclusions>-->
</dependency>

<!-- JollyDay Jackson — Jackson-based holiday data loader required by jollyday-core -->
<dependency>
  <groupId>de.focus-shift</groupId>
  <artifactId>jollyday-jackson</artifactId>
  <version>${jollyday.version}</version>
</dependency>

        <!-- ========================= -->
        <!-- Arquillian core API -->
        <!-- ========================= -->
        <dependency>
            <groupId>org.jboss.arquillian.container</groupId>
            <artifactId>arquillian-container-test-api</artifactId>
            <version>1.10.1.Final</version> <!-- mod 23-03-2026 was 1.10.0.Final -->
            <scope>test</scope>
        </dependency>

        <!-- ========================= -->
        <!-- Arquillian JUnit 5 -->
        <!-- ========================= -->
        <dependency>
            <groupId>org.jboss.arquillian.junit5</groupId>
            <artifactId>arquillian-junit5-container</artifactId>
            <version>${arquillian.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- ========================= -->
        <!-- ShrinkWrap -->
        <!-- ========================= -->
        <dependency>
            <groupId>org.jboss.shrinkwrap</groupId>
            <artifactId>shrinkwrap-api</artifactId>
            <version>${shrinkwrap.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.jboss.shrinkwrap</groupId>
            <artifactId>shrinkwrap-impl-base</artifactId>
            <version>${shrinkwrap.version}</version>
            <scope>test</scope>
        </dependency>

<!-- ShrinkWrap Maven resolver implementation — resolves Maven artifacts for Arquillian deployment archives -->
<dependency>
    <groupId>org.jboss.shrinkwrap.resolver</groupId>
    <artifactId>shrinkwrap-resolver-impl-maven</artifactId>
    <version>3.3.5</version>  <!-- mod 16-03-2026 was 3.3.4 -->
    <scope>test</scope>

</dependency>

<!-- ShrinkWrap Maven resolver API — Maven resolver contract (required by impl) -->
<dependency>
    <groupId>org.jboss.shrinkwrap.resolver</groupId>
    <artifactId>shrinkwrap-resolver-api-maven</artifactId>
    <version>3.3.5</version> <!-- mod 16-03-2026 was 3.3.4 -->
    <scope>test</scope>
</dependency>

<!-- ShrinkWrap Maven archive resolver — resolves Maven artifacts into ShrinkWrap archives -->
<dependency>
    <groupId>org.jboss.shrinkwrap.resolver</groupId>
    <artifactId>shrinkwrap-resolver-impl-maven-archive</artifactId>
    <version>3.3.5</version>  <!-- mod 16-03-2026 was 3.3.4 -->
    <scope>test</scope>
</dependency>

        <!-- ========================= -->
        <!-- WildFly Arquillian (REMOTE) -->
        <!-- ========================= -->

<dependency>
    <groupId>org.wildfly.arquillian</groupId>
    <artifactId>wildfly-arquillian-container-remote</artifactId>
    <version>6.0.0.Alpha1</version>  <!-- existe autre version  -->
    <scope>test</scope>
</dependency>

<!-- Log4j2 SLF4J bridge — routes SLF4J calls to Log4j2 during Arquillian/WildFly tests -->
<dependency>
  <groupId>org.apache.logging.log4j</groupId>
  <artifactId>log4j-slf4j2-impl</artifactId>
  <version>${log4j2.version}</version>
  <scope>test</scope>
</dependency>

<!-- WildFly client all — WildFly remote management client required by Arquillian remote container -->
<dependency>
    <groupId>org.wildfly</groupId>
    <artifactId>wildfly-client-all</artifactId>
    <version>39.0.1.Final</version>  <!-- mod 16-03-2026 was 39.0.0.Final -->
    <scope>test</scope>
</dependency>

<!-- weld-se-core désactivé 2026-04-03 — aucun import trouvé dans les tests. Les tests utilisent
     Arquillian (@ExtendWith(ArquillianExtension.class)), pas Weld SE standalone. Résidu d'une
     tentative de tests CDI unitaires abandonnée.
<dependency>
    <groupId>org.jboss.weld.se</groupId>
    <artifactId>weld-se-core</artifactId>
    <version>${weld.version}</version>
    <scope>test</scope>
</dependency>
-->

<!-- jakarta.enterprise.cdi-api désactivé 2026-04-03 — doublon : déjà inclus transitivement
     via jakarta.jakartaee-api (scope provided). Ajouter les deux crée une ambiguïté de version.
<dependency>
   <groupId>jakarta.enterprise</groupId>
   <artifactId>jakarta.enterprise.cdi-api</artifactId>
   <version>4.1.0</version>
   <scope>provided</scope>
</dependency>
-->

<!-- WildFly Controller Client — WildFly management API for deployment/monitoring operations -->
<dependency>
    <groupId>org.wildfly.core</groupId>
    <artifactId>wildfly-controller-client</artifactId>
    <scope>provided</scope>
    <version>32.0.0.Beta6</version>  <!-- mod 16-03-2026 was 31.0.1.Final -->
</dependency>
<!-- JBoss DMR — Dynamic Model Representation used with wildfly-controller-client management API -->
<dependency>
    <groupId>org.jboss</groupId>
    <artifactId>jboss-dmr</artifactId>
    <version>1.7.0.Final</version>
</dependency>

<!-- MaxMind GeoIP2 — IP geolocation to auto-detect player country/language at login (DB at C:/opt/geo/) -->
<dependency>
    <groupId>com.maxmind.geoip2</groupId>
    <artifactId>geoip2</artifactId>
    <version>5.0.2</version>
</dependency>

<!-- Déjà inclus, utilisez scope provided -->
<dependency>
    <groupId>org.eclipse.microprofile.config</groupId>
    <artifactId>microprofile-config-api</artifactId>
    <version>3.1</version>
    <scope>provided</scope>
</dependency>
<!-- Fix warning: Cannot find annotation method 'resolution()' in type 'Requirement' -->
<dependency>
    <groupId>org.osgi</groupId>
    <artifactId>org.osgi.annotation.bundle</artifactId>
    <version>2.0.0</version>
    <scope>provided</scope>
</dependency>

<!-- WildFly Core Galleon pack — WildFly provisioning BOM (resolves wildfly-controller-client transitive deps) -->
<dependency>
    <groupId>org.wildfly.core</groupId>
    <artifactId>wildfly-core-galleon-pack</artifactId>
    <version>32.0.0.Beta6</version>  <!-- mod 16-03-2026 was 31.0.1.Final -->
    <type>pom</type>
    <scope>provided</scope>
</dependency>
<!-- OSGi versioning annotation — suppresses Maven "Cannot find annotation method 'version()'" warnings -->
<dependency>
    <groupId>org.osgi</groupId>
    <artifactId>org.osgi.annotation.versioning</artifactId>
    <version>1.1.2</version>
    <scope>provided</scope>
</dependency>

<!-- weld-junit5 désactivé 2026-04-03 — aucun import trouvé dans les tests. Les tests d'intégration
     passent par Arquillian remote (WildFly), les tests unitaires par Mockito. Aucune classe
     n'utilise @WeldSetup ou l'extension Weld JUnit 5.
<dependency>
    <groupId>org.jboss.weld</groupId>
    <artifactId>weld-junit5</artifactId>
    <version>5.0.3.Final</version>
    <scope>test</scope>
</dependency>
-->
 </dependencies>
 
 
<build>
    <!--  enlevé 22-01-2026 used for compilation protobuf-maven-plugin 
    <finalName>${finalName.version}</finalName>
        <extensions>
            <extension>
                <groupId>kr.motd.maven</groupId>
                <artifactId>os-maven-plugin</artifactId>
                <version>1.7.1</version>   
            </extension>
        </extensions>
-->
  <plugins>
 <!--  pour forcer version à exécuter -->  
    <plugin>
     <groupId>org.apache.maven.plugins</groupId>
     <artifactId>maven-clean-plugin</artifactId>
     <version>${maven-clean-plugin.version}</version>
     <configuration>
        <failOnError>true</failOnError>
      </configuration>
</plugin>
<plugin>
     <groupId>org.apache.maven.plugins</groupId>
     <artifactId>maven-resources-plugin</artifactId>
     <version>${maven-resources-plugin.version}</version>
     <configuration>
         <propertiesEncoding>${project.build.sourceEncoding}</propertiesEncoding>
     </configuration>
</plugin>
<plugin>
  <!--  https://www.talksinfo.com/installing-3rd-party-jars-maven/-->
     <groupId>org.apache.maven.plugins</groupId>
     <artifactId>maven-install-plugin</artifactId>
     <version>${maven-install-plugin.version}</version>
</plugin>

<plugin>
                <groupId>org.wildfly.plugins</groupId>
                <artifactId>wildfly-maven-plugin</artifactId>
                <version>${maven-wildfly-plugin.version}</version>
                <configuration>
     <!--  If force is set to true, the server is queried to see if the application already exists.
           If the application already exists, the application is redeployed instead of deployed.  -->
                    <force>true</force>
                    <hostname>localhost</hostname> <!-- added 11-12-2025 default -->
                    <port>9990</port> <!-- added 11-12-2025 default -->
                    <javaOpts>-Dapp.devMode=true</javaOpts> <!-- added 14/01/2026 used in ErrorController -->
                 </configuration>
                <executions>
                    <execution>
                        <id>package-deploy-only-lc</id>
                        <phase>package</phase> <!-- mod 13-01-2026 was install -->
                        <goals>
                           <goal>deploy-only</goal> <!-- was deploy-only -->
                        </goals>
                    </execution>
                    <!-- new 16-09-2023 remplace le copy manuel dans wildfly/standalone/deployments ?? à vérifier !!
                     <execution>
                        <id>id-deploy-driver-mysql</id>
                        <phase>install</phase>
                        <configuration>
                            <groupId>com.mysql</groupId>
                            <artifactId>mysql-connector-j</artifactId>
                            <name>mysql-connector-j-${mysql-connector-j.version}.jar</name>
                        </configuration>
                        <goals>
                            <goal>deploy-artifact</goal>
                        </goals>
                    </execution>-->
                 </executions>
     </plugin>

 <plugin>
    <groupId>org.apache.maven.plugins</groupId>
    <artifactId>maven-war-plugin</artifactId>
    <version>${maven-war-plugin.version}</version>
    <configuration>
        <archive>
            <addMavenDescriptor>true</addMavenDescriptor>
            <forced>true</forced>
            <manifest>
                <addDefaultEntries>true</addDefaultEntries>
                <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                <addDefaultSpecificationEntries>true</addDefaultSpecificationEntries>
                <addBuildEnvironmentEntries>true</addBuildEnvironmentEntries>
            </manifest>
            <manifestEntries>
                <!-- Informations custom pour votre application -->
                <netbeans>${netbeans.version}</netbeans>
                <primeflex>${primeflex.version}</primeflex>
                <javaEE>${jakartaee-api.version}</javaEE>
                <omnifaces>${omnifaces.version}</omnifaces>
                <PFextension>${primefaces.extension.version}</PFextension>
                <logging>${log4j2.version}</logging>
                <buildTime>${timestamp}</buildTime>
                <buildTimestamp>${maven.build.timestamp}</buildTimestamp>
                <application>Application GolfLC - version 3.0 - WHS compatible</application>
            </manifestEntries>
        </archive>
        <packagingExcludes>pom.xml</packagingExcludes>
        <failOnMissingWebXml>false</failOnMissingWebXml>
    </configuration>
</plugin>
 <!--  https://stackoverflow.com/questions/13228472/how-to-access-maven-build-timestamp-for-resource-filtering
 https://www.mojohaus.org/build-helper-maven-plugin/usage.html
 utilisé pour avoir le build time en local time
-->
  <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>build-helper-maven-plugin</artifactId>
            <version>${maven.build.helper.plugin.version}</version>
            <executions>
                <execution>
                    <id>id-timestamp-property</id>
                    <goals>
                        <goal>timestamp-property</goal>
                    </goals>
                    <configuration>
                        <name>timestamp</name> <!-- used by program <buildT> property in MANIFEST.MF  -->
                        <pattern>${maven.build.timestamp.format}</pattern>
                        <timeZone>Europe/Brussels</timeZone>
                    </configuration>
                </execution>
            </executions>
        </plugin>          
<!-- enlevé 26-01-2026https://maven.apache.org/plugins/maven-dependency-plugin/index.html
       <plugin> 
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>${maven-dependency-plugin.version}</version>
                <executions>
                    
                    <execution>
                        <id>copy-dependencies</id>
                         <phase>package</phase>
                         <goals>
                             <goal>copy-dependencies</goal>
                         </goals>
                         <configuration>
                            <outputDirectory>${project.build.directory}/allDependencies</outputDirectory>
                            <overWriteReleases>false</overWriteReleases>
                            <overWriteSnapshots>false</overWriteSnapshots>
                            <overWriteIfNewer>true</overWriteIfNewer>
                            <excludeTransitive>false</excludeTransitive>
                        </configuration>
                    </execution>
                    
                    <execution>
                        <id>id-dependency-lc</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${endorsed.dir}/dependenciesJakartaOnly</outputDirectory>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>jakarta.platform</groupId>
                                    <artifactId>jakarta.jakartaee-api</artifactId>
                                    <version>${jakartaee.version}</version>
                                    <type>jar</type>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
-->            


          
          
          
          
      <!--      <suiteXmlFiles>
                <suiteXmlFile>/src/test/resources/testng.xml</suiteXmlFile>
          </suiteXmlFiles>  
           <enableAssertions>true</enableAssertions>
            <argLine>-enable-preview</argLine>
            <includes> 
              <include>**/*Test.java</include>
            </includes>
            <parallel>classes</parallel>
            <test>${test.file}</test>

          -->


<!-- 04-01-2026Plugin pour générer le rapport HTML -->
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-report-plugin</artifactId>
            <version>3.5.5</version>  <!-- mod 16-03-2026 was 3.5.4 -->
            <configuration>
                <outputDirectory>${project.build.directory}/surefire-html-report</outputDirectory>
            </configuration>
        </plugin>
<!-- enlevé le 10-21-20264-06-2022  pour gRPC
    <plugin>
      <groupId>org.xolstice.maven.plugins</groupId>
      <artifactId>protobuf-maven-plugin</artifactId>
      <version>0.6.1</version>
      <configuration>
        <protocArtifact>
            com.google.protobuf:protoc:${grpc.version}:exe:${os.detected.classifier}
        </protocArtifact>
        <pluginId>grpc-java</pluginId>
        <pluginArtifact>
            io.grpc:protoc-gen-grpc-java:${protoc.version}:exe:${os.detected.classifier}
        </pluginArtifact>
      </configuration>
      <executions>
        <execution>
            <id>id-protobuf-lc</id>
          <goals>
            <goal>compile</goal>
            <goal>compile-custom</goal>
          </goals>
        </execution>
      </executions>
    </plugin>
   --> 
    <!-- new 30/08/2022 pour execution RUN
 https://www.digitalocean.com/community/tutorials/exec-maven-plugin-run-java-programs-maven-build
  https://www.mojohaus.org/exec-maven-plugin/usage.html 
https://github.com/mojohaus/exec-maven-plugin/releases    -->
   <plugin>
		<groupId>org.codehaus.mojo</groupId>
		<artifactId>exec-maven-plugin</artifactId>
		<version>${maven-exec-plugin.version}</version>
                <executions>
                    <execution>
                        <id>run-app</id>
                        <goals>
                            <goal>java</goal>
                        </goals>
                        <configuration>
                        <environmentVariables>
                            <LANG>en_US</LANG>
                            <API_URL>https://example.com</API_URL>
                            <MY_VAR>HelloWorld</MY_VAR>
                        </environmentVariables>
                        </configuration>
                    </execution>
                </executions>
		<configuration>
               <!--  enlevé 30-01-2026 11:47    <executable>maven</executable>  mod 03-12-2025 <executable>java</executable>  -->
		    <mainClass>${exec.mainClass.pom}</mainClass>
                    <longClasspath>true</longClasspath> <!-- new 31-01-2025 -->
                    <cleanupDaemonThreads>false</cleanupDaemonThreads>
                        <arguments>
                            <argument>--enable-preview</argument>
                            <argument>foo</argument>
                            <argument>bar</argument>
                        </arguments>
                        <systemProperties>
                            <systemProperty>
                                <key>env</key>
                                <value>${env}</value>
                            </systemProperty>
                            <systemProperty>
                                <key>mode</key>
                                <value>dev</value>
                            </systemProperty>
                        </systemProperties>
		</configuration>
    </plugin>

<!-- Sets minimal Maven version -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>${maven-enforcer-plugin.version}</version>
                <executions>
                    <execution>
                        <id>id-enforce-lc</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireMavenVersion>
                                    <version>3.9.11</version>
                                    <message>From enforcer-plugin : Invalid Maven version. It should, at least, be 3.9.11</message>
                                </requireMavenVersion>
                                <banDuplicatePomDependencyVersions/>
                                <requireJavaVersion>
                                    <version>${java.version}</version>
                                    <message>From enforcer-plugin : Invalid Java version. It should, at least, be 25</message>
                                </requireJavaVersion>
                                <requireOS>
                             <!--       <name>Windows 11</name>  -->
                                    <family>windows</family>
                              <!--      <arch>amd64</arch>  -->
                              <!--      <version>10.0</version> -->
                                 </requireOS>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
 <!-- 14-09-2025 sert à rien ?  -->
   <plugin>
    <groupId>org.apache.maven.plugins</groupId>
    <artifactId>maven-compiler-plugin</artifactId>
    <version>${maven-compiler-plugin.version}</version>
    <configuration>
    <fork>true</fork>
    <useIncrementalCompilation>true</useIncrementalCompilation>
 <!--    mod 13-01-2026<meminitial>256m</meminitial>
     <maxmem>2048m</maxmem>
     -->
     <release>26</release>
     <encoding>${project.build.sourceEncoding}</encoding>
    <compilerArgs>
        <arg>-Xlint:all,-serial,-path</arg>
        <arg>-Xmaxerrs</arg>
        <arg>50</arg>
        <arg>-J-Duser.language=fr</arg>
        <arg>-J-Duser.country=FR</arg>
        <arg>--enable-preview</arg>
        <arg>-J-Xms1024m</arg>
        <arg>-J-Xmx2048m</arg>
    </compilerArgs>
     <showDeprecation>true</showDeprecation>
     <showWarnings>true</showWarnings>
     <failOnWarning>false</failOnWarning>
     <verbose>false</verbose>
     <debug>true</debug>
     <debuglevel>lines,vars,source</debuglevel>
     <enablePreview>true</enablePreview>
     <proc>none</proc>
     <parameters>true</parameters>   <!--Parameters prservés pour CDI/JAX-RS-->
    </configuration>
    
   </plugin>
   <!-- new 26-01-2026 pour test intégration cdi/weld
     Failsafe regarde tout ce qui est sous src/test/java et ensuite filtre selon le pattern (*IT.java par convention) ou le groupe/tag.
    Par convention, Maven Failsafe exécute automatiquement tous les tests dont le nom correspond à :
    **/*IT.java
    **/*IT*.java
    **/*ITCase.java 
Les tests d’intégration seront exécutés lors de mvn verify ou mvn integration-test.   -->
   <plugin>
    <artifactId>maven-failsafe-plugin</artifactId>
    <version>${failsafe.version}</version>
    <executions>
        <execution>
            <goals>
                <goal>integration-test</goal>
                <goal>verify</goal>
            </goals>
            <configuration>
                <includes>
                    <include>**/*IT.java</include>       <!-- convention integration tests -->
                </includes>
              <!--   <includeTags>integration</includeTags>  Compatible JUnit 5✔ Fonctionne avec @Tag("integration")  -->
                <forkCount>1</forkCount>  <!-- exécute tests dans la JVM actuelle -->
                <reuseForks>false</reuseForks> <!-- optionnel : ne réutilise pas de forks -->
                <skipTests>${skip.integration.tests}</skipTests>
                <skipITs>${skip.integration.tests}</skipITs>
                <argLine>--enable-preview</argLine>
            </configuration>
        </execution>
    </executions>
</plugin>

   <!-- ajouté 21-05-2020  va avec Junit5-->
<plugin>
	<artifactId>maven-surefire-plugin</artifactId>
	<version>${maven-surefire-plugin.version}</version>
       <configuration>
      <!--    <skipTests>${tests.skip}</skipTests>  -->
          <skipTests>${skip.unit.tests}</skipTests>
          <systemPropertyVariables>
         <!--   <jboss.home>${env.JBOSS_HOME}</jboss.home>    04-01-2026 utilisée dans arquillian.xml  -->
          </systemPropertyVariables>
          <argLine>-Dfile.encoding=UTF-8 --enable-preview -XX:+EnableDynamicAgentLoading</argLine>
          <includes>
                <include>**/*Test.java</include><!-- convention unit tests -->
          </includes>
           <excludedGroups>integration</excludedGroups> <!-- voir failsafe !! exclut explicitement les tests integration -->
          </configuration>
</plugin>        

  </plugins>
 
<!--  12-04-2020 15:21 https://blog.frankel.ch/whats-the-version-of-my-deployed-application/   -->
<resources>
   <resource>
      <directory>${project.basedir}/src/main/resources</directory>
      <filtering>true</filtering>
   </resource>
</resources>
 </build>
    
  <!-- ✅ PROFILES - utilisé dans nbactions.xml Ajoutez ici, juste après </build> -->
<profiles>
    <profile>
        <id>fast-it</id>
        <properties>
            <skip.unit.tests>true</skip.unit.tests>
            <skip.integration.tests>false</skip.integration.tests>
        </properties>
    </profile>
    
    <profile>
        <id>fast-ut</id>
        <properties>
            <skip.unit.tests>false</skip.unit.tests>
            <skip.integration.tests>true</skip.integration.tests>
        </properties>
    </profile>
    
</profiles>
        <!--
    <profile>
        <id>fast-it</id>
        <properties>
 
                  <skip.unit.tests>true</skip.unit.tests>
                  <skip.integration.tests>false</skip.integration.tests>
        </properties>
        <build>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <configuration>

                        <forkCount>1</forkCount>
                        <reuseForks>true</reuseForks>
                        <useFile>false</useFile>
                       
                    </configuration>
                </plugin>
            </plugins>
        </build>
    </profile>
    -->
     
   
    
    
<repositories>
    <!-- 1️⃣ Maven Central : source principale -->
    <repository>
        <id>central</id>
        <name>Maven Central Repository</name>
        <url>https://repo.maven.apache.org/maven2</url>
        <releases>
            <enabled>true</enabled>
        </releases>
        <snapshots>
            <enabled>false</enabled>
        </snapshots>
    </repository>

    <!-- JitPack : fallback pour les libs absentes de Central 
            JitPack configuré pour download de QRGen-->
    <repository>
        <id>jitpack.io</id>
        <name>JitPack Repository</name>
        <url>https://jitpack.io</url>
        <releases>
            <enabled>true</enabled>
        </releases>
        <snapshots>
            <enabled>true</enabled>
        </snapshots>
    </repository>
    
   <repository>
    <id>jboss-public-repository-group</id>
    <url>https://repository.jboss.org/nexus/content/groups/public/</url>
    <releases><enabled>true</enabled></releases>
    <snapshots><enabled>true</enabled></snapshots>
  </repository>
    
    
    
</repositories>

</project>