package validator;

//import entite.ScoreMatchplay;
import java.util.Arrays;
import javax.validation.ConstraintValidator;
import javax.validation.ConstraintValidatorContext;
/*
public class MatchplayValidator implements interfaces.Log, ConstraintValidator<Matchplay, String[][]>
{
//private String departement;

@Override
public void reset(Matchplay MP4)
{
   // departement = MP4.departement();
    // See JSR 303 Section 2.4.1 for sample implementation.
    // gives access to any attributes of the annotation such min/max fields of the size annotation
}

@Override
public boolean isValid(String[][] value, ConstraintValidatorContext context)
{
    LOG.info("isValid from MatchplayValidator = " + Arrays.deepToString(value));
//	if (value == null || value.length() == 0)
 //       {
//		return true;
//	}
return true; //value.substring(0, 1).equals(value.substring(0, 1).toUpperCase());
}

} // end Class
*/